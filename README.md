# zipFFT
Efficient zero implicit padded (zip) FFT kernels in CUDA with a PyTorch interface.

## Requirements

* Python 3.10 or above (check)
* Some version of CUDA and cuFFTDx (separate out runtime and compilation tools)